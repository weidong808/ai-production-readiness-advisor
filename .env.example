# OpenAI (server-only — never commit real keys, never expose to the client)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Optional overrides
# OPENAI_BASE_URL=https://api.openai.com/v1
# AI_NARRATIVE_ENABLED=true
# AI_RATE_LIMIT_PER_IP_PER_DAY=10
# AI_MAX_OUTPUT_TOKENS=1500
